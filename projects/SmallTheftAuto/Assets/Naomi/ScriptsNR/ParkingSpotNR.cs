using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParkingSpotNR : MonoBehaviour
{

    public bool hasCar;
    public GameObject carPrefabNR;
    
    
    
    // Start is called before the first frame update
    void Start()
    {
        if (hasCar)
        {
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
